// Keep this constant in a zero-dependency module so client code can import the
// sub-agent tool prefix without pulling the workflow runtime into the browser.
export const AGENT_TOOL_PREFIX = 'agent_'
